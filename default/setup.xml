<setup>
    <block title="Monitor CADORS Reports" endpoint="data/inputs/monitor">
        <text>Enter the full path to the directory that contains the CADORS data files. These files should all be in the native XML format that came from Transport Canada.</text>
        <!-- <input field="_new">
            <label>Path to CADORS data files [$name$] : $<field_name>$</label>
            <type>text</type>
        </input> -->
        <input entity="/" field="enabled">
            <label>Disabled by Default [$name$]:</label>
            <type>bool</type>
        </input>
    </block>

    <!-- <block title="Monitor CADORS Reports" endpoint="configs/conf-macros" entity="cadors_index">
        <text>Macro Settings</text>
        <input field="iseval">
            <label>Macro Value [$name$]:</label>
            <type>text</type>
        </input>
    </block> -->
</setup>